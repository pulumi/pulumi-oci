// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * This resource provides the Zone Promote Dnssec Key Version resource in Oracle Cloud Infrastructure DNS service.
 *
 * Promotes a specified `DnssecKeyVersion` on the zone.
 *
 * If the `DnssecKeyVersion` identified in the request body is a key signing key (KSK) that is replacing
 * another `DnssecKeyVersion`, then the old `DnssecKeyVersion` is scheduled for removal from the zone.
 *
 * For key signing keys (KSKs), you must create the DS record with the new key information **before** promoting
 * the new key to establish a chain of trust. To avoid a service disruption, remove the old DS record as soon
 * as its TTL (time to live) expires.
 *
 * For more information, see [DNSSEC](https://docs.cloud.oracle.com/iaas/Content/DNS/Concepts/dnssec.htm).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as oci from "@pulumi/oci";
 *
 * const testZonePromoteDnssecKeyVersion = new oci.dns.ZonePromoteDnssecKeyVersion("test_zone_promote_dnssec_key_version", {
 *     dnssecKeyVersionUuid: zonePromoteDnssecKeyVersionDnssecKeyVersionUuid,
 *     zoneId: testZone.id,
 *     scope: zonePromoteDnssecKeyVersionScope,
 * });
 * ```
 *
 * ## Import
 *
 * Import is not supported for this resource.
 */
export class ZonePromoteDnssecKeyVersion extends pulumi.CustomResource {
    /**
     * Get an existing ZonePromoteDnssecKeyVersion resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ZonePromoteDnssecKeyVersionState, opts?: pulumi.CustomResourceOptions): ZonePromoteDnssecKeyVersion {
        return new ZonePromoteDnssecKeyVersion(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'oci:Dns/zonePromoteDnssecKeyVersion:ZonePromoteDnssecKeyVersion';

    /**
     * Returns true if the given object is an instance of ZonePromoteDnssecKeyVersion.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ZonePromoteDnssecKeyVersion {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ZonePromoteDnssecKeyVersion.__pulumiType;
    }

    /**
     * The UUID of the `DnssecKeyVersion` that is being promoted.
     */
    public readonly dnssecKeyVersionUuid!: pulumi.Output<string>;
    /**
     * Specifies to operate only on resources that have a matching DNS scope.
     */
    public readonly scope!: pulumi.Output<string>;
    /**
     * The OCID of the target zone.
     *
     *
     * ** IMPORTANT **
     * Any change to a property that does not support update will force the destruction and recreation of the resource with the new property values
     */
    public readonly zoneId!: pulumi.Output<string>;

    /**
     * Create a ZonePromoteDnssecKeyVersion resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ZonePromoteDnssecKeyVersionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ZonePromoteDnssecKeyVersionArgs | ZonePromoteDnssecKeyVersionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ZonePromoteDnssecKeyVersionState | undefined;
            resourceInputs["dnssecKeyVersionUuid"] = state ? state.dnssecKeyVersionUuid : undefined;
            resourceInputs["scope"] = state ? state.scope : undefined;
            resourceInputs["zoneId"] = state ? state.zoneId : undefined;
        } else {
            const args = argsOrState as ZonePromoteDnssecKeyVersionArgs | undefined;
            if ((!args || args.dnssecKeyVersionUuid === undefined) && !opts.urn) {
                throw new Error("Missing required property 'dnssecKeyVersionUuid'");
            }
            if ((!args || args.zoneId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'zoneId'");
            }
            resourceInputs["dnssecKeyVersionUuid"] = args ? args.dnssecKeyVersionUuid : undefined;
            resourceInputs["scope"] = args ? args.scope : undefined;
            resourceInputs["zoneId"] = args ? args.zoneId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ZonePromoteDnssecKeyVersion.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ZonePromoteDnssecKeyVersion resources.
 */
export interface ZonePromoteDnssecKeyVersionState {
    /**
     * The UUID of the `DnssecKeyVersion` that is being promoted.
     */
    dnssecKeyVersionUuid?: pulumi.Input<string>;
    /**
     * Specifies to operate only on resources that have a matching DNS scope.
     */
    scope?: pulumi.Input<string>;
    /**
     * The OCID of the target zone.
     *
     *
     * ** IMPORTANT **
     * Any change to a property that does not support update will force the destruction and recreation of the resource with the new property values
     */
    zoneId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ZonePromoteDnssecKeyVersion resource.
 */
export interface ZonePromoteDnssecKeyVersionArgs {
    /**
     * The UUID of the `DnssecKeyVersion` that is being promoted.
     */
    dnssecKeyVersionUuid: pulumi.Input<string>;
    /**
     * Specifies to operate only on resources that have a matching DNS scope.
     */
    scope?: pulumi.Input<string>;
    /**
     * The OCID of the target zone.
     *
     *
     * ** IMPORTANT **
     * Any change to a property that does not support update will force the destruction and recreation of the resource with the new property values
     */
    zoneId: pulumi.Input<string>;
}
