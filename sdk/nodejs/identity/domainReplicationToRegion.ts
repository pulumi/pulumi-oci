// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * This resource provides the Domain Replication To Region resource in Oracle Cloud Infrastructure Identity service.
 *
 * Replicate domain to a new region. This is an asynchronous call - where, at start,
 * {@code state} of this domain in replica region is set to ENABLING_REPLICATION.
 * On domain replication completion the {@code state} will be set to REPLICATION_ENABLED.
 *
 * To track progress, HTTP GET on /iamWorkRequests/{iamWorkRequestsId} endpoint will provide
 * the async operation's status.
 *
 * If the replica region's {@code state} is already ENABLING_REPLICATION or REPLICATION_ENABLED,
 * returns 409 CONFLICT.
 * - If the domain doesn't exists, returns 404 NOT FOUND.
 * - If home region is same as replication region, return 400 BAD REQUEST.
 * - If Domain is not active or being updated, returns 400 BAD REQUEST.
 * - If any internal error occurs, return 500 INTERNAL SERVER ERROR.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as oci from "@pulumi/oci";
 *
 * const testDomainReplicationToRegion = new oci.identity.DomainReplicationToRegion("test_domain_replication_to_region", {
 *     domainId: testDomain.id,
 *     replicaRegion: domainReplicationToRegionReplicaRegion,
 * });
 * ```
 *
 * ## Import
 *
 * Import is not supported for this resource.
 */
export class DomainReplicationToRegion extends pulumi.CustomResource {
    /**
     * Get an existing DomainReplicationToRegion resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DomainReplicationToRegionState, opts?: pulumi.CustomResourceOptions): DomainReplicationToRegion {
        return new DomainReplicationToRegion(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'oci:Identity/domainReplicationToRegion:DomainReplicationToRegion';

    /**
     * Returns true if the given object is an instance of DomainReplicationToRegion.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DomainReplicationToRegion {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DomainReplicationToRegion.__pulumiType;
    }

    /**
     * The OCID of the domain
     */
    public readonly domainId!: pulumi.Output<string>;
    /**
     * A region for which domain replication is requested for. See [Regions and Availability Domains](https://docs.cloud.oracle.com/iaas/Content/General/Concepts/regions.htm) for the full list of supported region names.  Example: `us-phoenix-1` 
     *
     *
     * ** IMPORTANT **
     * Any change to a property that does not support update will force the destruction and recreation of the resource with the new property values
     */
    public readonly replicaRegion!: pulumi.Output<string>;

    /**
     * Create a DomainReplicationToRegion resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DomainReplicationToRegionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DomainReplicationToRegionArgs | DomainReplicationToRegionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DomainReplicationToRegionState | undefined;
            resourceInputs["domainId"] = state ? state.domainId : undefined;
            resourceInputs["replicaRegion"] = state ? state.replicaRegion : undefined;
        } else {
            const args = argsOrState as DomainReplicationToRegionArgs | undefined;
            if ((!args || args.domainId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'domainId'");
            }
            resourceInputs["domainId"] = args ? args.domainId : undefined;
            resourceInputs["replicaRegion"] = args ? args.replicaRegion : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DomainReplicationToRegion.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DomainReplicationToRegion resources.
 */
export interface DomainReplicationToRegionState {
    /**
     * The OCID of the domain
     */
    domainId?: pulumi.Input<string>;
    /**
     * A region for which domain replication is requested for. See [Regions and Availability Domains](https://docs.cloud.oracle.com/iaas/Content/General/Concepts/regions.htm) for the full list of supported region names.  Example: `us-phoenix-1` 
     *
     *
     * ** IMPORTANT **
     * Any change to a property that does not support update will force the destruction and recreation of the resource with the new property values
     */
    replicaRegion?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a DomainReplicationToRegion resource.
 */
export interface DomainReplicationToRegionArgs {
    /**
     * The OCID of the domain
     */
    domainId: pulumi.Input<string>;
    /**
     * A region for which domain replication is requested for. See [Regions and Availability Domains](https://docs.cloud.oracle.com/iaas/Content/General/Concepts/regions.htm) for the full list of supported region names.  Example: `us-phoenix-1` 
     *
     *
     * ** IMPORTANT **
     * Any change to a property that does not support update will force the destruction and recreation of the resource with the new property values
     */
    replicaRegion?: pulumi.Input<string>;
}
